services:
  arrview:
    build: .
    container_name: arrview
    restart: unless-stopped
    ports:
      - "3223:8080"
    environment:
      - TZ=${TZ:-Asia/Colombo}
      - ARRVIEW_DATA=/app/data
    volumes:
      - arrview-data:/app/data

volumes:
  arrview-data:
